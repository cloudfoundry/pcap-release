<%=
config = {
  "log_level" => p("pcap-server.log_level"),
  "listen" => p("pcap-server.listen"),
  "container_store" => p("pcap-server.container_store"),
  "runc" => p("pcap-server.runc"),
  "runc_root" => p("pcap-server.runc_root"),
  "cert" => "/var/vcap/jobs/pcap-server/config/certs/pcap-server.crt",
  "key" => "/var/vcap/jobs/pcap-server/config/certs/pcap-server_private.key",
  "ca_cert" => "/var/vcap/jobs/pcap-server/config/certs/client_ca.crt",
}
YAML.dump(config)
%>

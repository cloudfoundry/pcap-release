<%=
config = {
  "id" => p("pcap-agent.id"),
  "log_level" => p("pcap-agent.log_level"),
  "listen" => {
    "port" => p("pcap-agent.listen.port"),
    "tls" => {
      "certificate"=> "/var/vcap/jobs/pcap-agent/config/certs/pcap-agent.crt",
      "private_key" => "/var/vcap/jobs/pcap-agent/config/certs/pcap-agent.key",
      "client_cas" => "/var/vcap/jobs/pcap-agent/config/certs/client-ca.crt",
    },
  },
  "buffer" => {
    "size" => p("pcap-agent.buffer.size"),
    "upper_limit" => p("pcap-agent.buffer.upper_limit"),
    "lower_limit" => p("pcap-agent.buffer.lower_limit"),
  },
}
YAML.dump(config)
%>
